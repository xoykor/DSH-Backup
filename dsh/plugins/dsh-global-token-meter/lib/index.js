/**
 * The token-speed meter is a browser-only surface plugin. Keeping the host
 * half empty makes it available from the Web composition without adding a
 * second agent-plane service to every preset.
 */
export function apply() {}
