name: Feature request
description: Suggest an improvement for dsh-score
title: '[Feature] '
labels: ['enhancement']
body:
  - type: textarea
    id: problem
    attributes:
      label: Problem to solve
      description: What problem would this feature solve? Describe the situation, not the solution.
      placeholder: e.g. I want to compare a score across harness versions, but each card only records the producer version.
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
      description: What the tool/command/report should do once implemented.
    validations:
      required: true
  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives considered (optional)
      description: Workarounds or alternative designs you already evaluated.
    validations:
      required: false
  - type: textarea
    id: context
    attributes:
      label: Additional context (optional)
      description: Anything else that helps — links, related issues, example cards (redact secrets first).
    validations:
      required: false
