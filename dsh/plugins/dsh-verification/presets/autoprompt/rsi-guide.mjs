/**
 * rsi-guide — DISABLED injection (stability fallback).
 * See enforce-guide for the reason. Guidance is carried by the adapter steer text.
 */

export const name = 'rsi-guide'

export function apply(ctx, config) {
  void ctx
  void config
}