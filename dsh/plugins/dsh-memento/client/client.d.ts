// client/client.d.ts — dsh-memento Web 面板客户端模块（最小类型声明）。
//
// client.js 是副作用模块：执行时经 window.__ModuleLoader__.load 注册工厂，
// 物化时挂载只读观察面板；不导出任何值。本声明只为 NodeNext 解析
// `dsh-memento/client` 子路径提供类型锚点。
export {}
